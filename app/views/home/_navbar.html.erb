<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path do %>
        <%= image_tag ('/logo.png'), class: 'logo smaller-image-mobile' %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <%= link_to "Users", bot_users_path, class: 'nano-nav' %>
        <% if user_signed_in? %>
          <%= link_to "PDF", pdfs_path, class: 'nano-nav' %>
        <% end %>

        <% if user_signed_in? %>
          <div class="dropdown div-inline">
            <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
              &#9776;
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
              <li><%= link_to 'Account settings', edit_user_registration_path, class: 'nano-nav' %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nano-nav'  %></li>
            </ul>
          </div>
          <% else %>
              <%= link_to 'Sign up', new_user_registration_path, class: 'nano-nav' %>
        <% end %>                        


      </ul>
    </div>
  </div>
</nav>
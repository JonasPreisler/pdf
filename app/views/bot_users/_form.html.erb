<%= form_with(model: bot_user, local: true) do |form| %>
  <% if bot_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bot_user.errors.count, "error") %> prohibited this bot_user from being saved:</h2>

      <ul>
      <% bot_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :messenger_user_id %>
    <%= f.text_field :messenger_user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
